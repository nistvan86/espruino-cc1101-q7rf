{
  "name": "q7rf-mqtt",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "preinstall": "cd firmware; npm install; cd ../portal; npm install",
    "build": "cd firmware; npm run build; cd ../portal; npm run build",
    "flash": "espruino --no-ble --board ESP8266_4MB -b 115200 --port /dev/ttyUSB0 --config SAVE_ON_SEND=1 ./firmware/dist/app.js"
  },
  "author": "István Nagy <nistvan.86@gmail.com> (https://github.com/nistvan86)",
  "license": "ISC",
  "devDependencies": {
    "espruino": "^0.1.30"
  }
}
